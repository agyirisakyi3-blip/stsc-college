{
  "buildCommand": "PRISMA_SCHEMA=prisma/schema.pg.prisma npx prisma generate && node scripts/patch-prisma-client.mjs && vite build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
